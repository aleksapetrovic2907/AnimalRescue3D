using UnityEngine;

namespace Aezakmi.Carriers
{
    // Each carrier will have a follow player script,
    // however, drones and jeeps would behave differently in following
    // therefore, we have a base follow player script
    public abstract class CarrierFollowPlayerBase : MonoBehaviour
    {
    }
}
